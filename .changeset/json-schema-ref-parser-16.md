---
'@skillit/mcp': patch
---

Bump `@apidevtools/json-schema-ref-parser` from `^15.3.5` to `^16.0.3` (runtime dependency). Verified `$RefParser.dereference()` API, default export, and the resolver-disabled error path (`UnmatchedResolverError`) used by `resolveSchema()` are unchanged; all schema-ref tests pass.
